environment: prod
baseurl: stemsible.com
basedir: /apps/stemsible
serverurl: localhost:5000

database:
   engine : mysql
   db     : stemsible
   host   : localhost
   user   : stemsible
   passwd : $STEM_DBPASS
   dictcursor: True

logging:
   dir: /var/log/stemsible
   filename: /var/log/stemsible/stemsible.log
   level: DEBUG

sessions:
   dir: /data/stemsible/sessions

emails:
   name: Stemsible
   username: info@stemsible.com
   password: $STEMSIBLE_EMAIL_PASS
